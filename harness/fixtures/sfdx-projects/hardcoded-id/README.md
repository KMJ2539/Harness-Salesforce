# hardcoded-id

INTENTIONALLY VULNERABLE — harness-sf test fixture only. NOT for deployment.

Uses the fake SF ID format (`001FIXTURE000000001`) — distinguishable from real ID format. Verifies that the lint rule catches 18-char SF ID literals.
