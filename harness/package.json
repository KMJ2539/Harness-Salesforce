{
  "name": "@harness-sf/harness",
  "version": "0.1.0",
  "private": true,
  "description": "harness-sf measurement & verification infrastructure — a separate package isolated from the installer",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "zod": "^3.23.8"
  }
}
